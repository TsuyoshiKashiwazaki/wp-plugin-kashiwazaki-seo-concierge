# Changelog

本プロジェクトの主な変更点を記録します。フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に準拠し、[セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

## [1.0.2] - 2026-08-17

### Added

- 「分析」タブの履歴を削除できるようになりました。各行の「削除」で1件ずつ消せるほか、絞り込み（すべて／訪問者のみ／管理者テストのみ）に応じた一括削除ボタンを追加しています。「管理者テストのみ」に切り替えてから一括削除すれば、テストで作った履歴だけをまとめて消せます。どちらも確認ダイアログが出ます（削除は取り消せません）。削除すると「よく聞かれる質問」「コンテンツギャップ」の集計にも即座に反映されます。
- AI の呼び出しが失敗したとき、その理由（サービス名・モデル名・時刻・エラー内容）を管理画面の上部に表示するようになりました。これまでは訪問者側に「該当するページが見つかりませんでした。」と出るだけで、管理者からは原因が見えませんでした。上限に達して停止した場合やAPIキーが未設定の場合も表示します。次に成功すると自動で消えます。
- チャットの「考えています…」に回転するインジケーターを追加しました。最初の質問は回答生成に数秒かかるため、文字だけでは止まって見えていました。テーマカラーに追従し、動きを減らす設定の環境では明滅表示に切り替わります。

### Changed

- 「最新のレポート」のように新しさを尋ねる質問で、実際にいちばん新しいページを案内するようになりました。これまでは意味の近さだけで候補を選んでいたため、毎月更新される同じ形式のページが並ぶサイトでは、どれも似た内容と判定されて順番が実質的に決まらず、本文に「最新」と書いてある古いページのほうが上位に来ることがありました。新しさを尋ねる質問のときだけ候補をふだんより広く集め、そのなかを更新日の新しい順に並べ替えます。通常の質問の並び順・費用は変わりません。
- 回答AIに渡す候補に、そのページの最終更新日を添えるようになりました。これまで日付を渡していなかったため、回答AIは新旧を判断できませんでした。あわせて、新しさを尋ねられたときは日付の新しい候補を選び、その日付を回答文に示すようになります。
- API キーを「使うサービスごと」に保存するようになりました。これまでは検索AI・回答AIそれぞれにキーの保管枠が1つしかなく、使うサービスを切り替えても前のサービスのキーが残るため、切り替えるたびに認証エラー（401）で回答が止まっていました。OpenAI・GLM・Ollama Cloud のキーをそれぞれ覚えておくので、一度入力すれば行き来しても入れ直し不要です。既存のキーは更新時に自動で、その時点で選ばれているサービスの枠へ移動します。
- サービス・接続先・モデル名を1組として保存するようになりました。モデル名が空欄のまま保存しようとした場合は、その3項目とAPIキーを変更前のまま維持してエラーを表示します。以前はサービスだけが切り替わり、前のサービスのモデル名が残った状態で保存できました。
- 選んでいるサービスのキーが未入力のとき、管理画面の「APIキー」欄が「設定済み」ではなく未設定と表示されるようになりました。
- Ollama Cloud を選んだときに、モデル名を自動入力しないようにしました。これまで候補として入っていた `qwen3-coder:480b` は 2026-07-15 に提供終了しており、そのまま保存すると HTTP 410 で回答できませんでした。Ollama Cloud は提供モデルの入れ替えが多いため、空欄にして ollama.com の一覧から選んで入力する形にしています。サービスを切り替えたときにモデル名欄が前のサービスの値のまま残ることもなくなりました。
- 訪問者への同意文と、プライバシーポリシーの推奨文が、実際の送信先サービス名を表示するようになりました。これまでは設定に関係なく固定のサービス名を表示していたため、別のサービスを選んでいると、同意した送信先と実際の送信先が食い違っていました。
- 月額定額のサービス（Ollama Cloud など）を、従量課金として金額計算しないようになりました。これまでは価格表にないモデルを一律で高価なモデルの単価として積算していたため、実際には超過していないのに金額上限に達してチャットが停止する可能性がありました。定額サービスでは回数上限・トークン上限が歯止めとして働きます。
- 「優先して表示するURL」が検索結果の並び順に反映されるようになりました（関連度が同点のときに優先）。これまでは代替ページの提示にしか使われておらず、説明と実装が食い違っていました。ルールを保存すると既存のページにも即座に反映します（大規模サイトではバックグラウンドで処理）。
- 回答キャッシュが、使用サービス・接続先・口調の指示・候補件数など回答に影響する設定を含めて管理されるようになりました。これまではモデル名しか見ていなかったため、設定を変更しても同じ質問には最大24時間、変更前の回答が返っていました。ページの本文を更新した場合も、そのページを案内していた回答を破棄します。
- 「今すぐ再構築」の通知が実態に合うようになりました。1回の実行では一部のページしか処理されず残りはバックグラウンドで進むため、完了と断定せず進捗の確認先を案内します。
- 課金上限がすべて 0 のときの警告条件を、金額・トークンの上限に限定しました。回数上限だけでは1回あたりの金額を抑えられないためです。あわせて回数上限の説明文を、実際の数え方（インデックス再構築や再試行も含む API 呼び出し数）に合わせました。
- 接続先URLの入力欄で、既定値を初期値ではなくプレースホルダーとして表示するようになりました。空欄のまま保存すれば、今後サービス側の既定URLが変わっても自動で追従します。
- 連投制限をデータベース上の不可分な処理に変更しました。同時に届いた複数のリクエストが同じカウントを読んで一斉に通過することがなくなります。
- 管理画面のテスト（サンドボックス）が、公開用の回答キャッシュを読み書きしないようになりました。調整中の回答が訪問者へ配信されることがなくなります。
- 混雑時の再試行が、サービス側の指定する待機時間を尊重するようになりました。

### Fixed

- 挨拶や雑談への返答が、AI の返答を捨てて「該当するページが見つかりませんでした。」の定型文になる問題を修正。このパスだけプロンプトに JSON 形式の指示がなく、回答AIに `response_format` を無視するサービスを使うと、AI が正しく生成した返答を形式チェックで破棄していました。この返答はページを案内しない文章のみで構造化が不要なため、プレーンテキストとして受け取るようにしています。
- 検索AIの設定を変更したあと、インデックスの再構築が予約されず、次の定期実行まで検索結果が揃わない問題を修正。
- API キーの暗号化に失敗する環境で、入力したキーだけでなく保存済みのキーまで空で上書きされ、しかも成功通知が出ていた問題を修正。失敗時は保存を中止してエラーを表示します。
- マルチバイト文字列の拡張機能がない環境で、管理画面の「分析」タブが致命的エラーになる問題を修正。
- 削除の確認ダイアログが、コンテンツセキュリティポリシーを設定した環境で表示されず、ボタンを押した瞬間に削除される問題を修正。
- ページの言語判定が、URL 内の任意の2文字（`/ai/` や `/qa/` など）を言語コードと誤認し、該当ページが検索結果から除外される問題を修正。既存のインデックスも更新時に是正します。
- ベクトルデータの一時保存が、規模の大きいサイトで無言のうちに失敗し、質問のたびに再計算が走る問題を修正。
- アンインストール時の削除漏れを修正。マルチサイトでは各サイトを走査するようになり、質問履歴（IP アドレス・ブラウザ情報を含む）が残らなくなりました。設定値・一時データも接頭辞で一括削除します。
- プラグインを停止したときに、リンク到達性チェックの予約と実行状態が残る問題を修正。
- モデル名を空欄のまま保存でき、以後の問い合わせが必ず失敗する問題を修正。

### Security

- llms.txt に記載されたURLが安全性チェックを通らず登録され、到達性チェックがそのURLを保護なしで追跡していた問題を修正。ループバックアドレスや私設IPを外部から探索できる状態でした。登録前に検査し、到達性チェックも保護付きの取得に置き換えています。
- クリック記録が、案内していない任意のURLで上書きできた問題を修正。索引済みのURLのみ受け付けます。

## [1.0.1] - 2026-06-30

### Added

- リンク到達性チェック。索引したページが実際に開けるか（HTTP ステータス）を定期的に確認し、見つからない（404）・エラー（403・500・タイムアウト等）のページを回答候補から自動で外します。復旧すると自動で戻ります。一時的な不調で正常なページを誤って外さないよう、404・410 は即時、その他のエラーは2回続けて失敗したときに外します。管理画面「インデックス」タブに状態の内訳とリンク切れ一覧、手動チェックボタンを追加。
- スパム対策のための IP アドレス・ブラウザ（ユーザーエージェント）記録（任意・既定で有効）。質問ログに記録し、保存日数の経過で自動削除します。Cloudflare 経由のサイト向けに、実際の訪問者 IP を正しく判定する設定を追加（CF の公開 IP レンジ照合つき）。これにより連投制限も訪問者単位で正しく機能します。汎用リバースプロキシ向けの信頼プロキシ設定も追加。
- チャット会話のタブ内保持。ページの再読み込みや遷移をしても、タブを閉じるまで会話が残るようになりました（ブラウザの sessionStorage を使用）。

### Changed

- 管理画面「インデックス」タブのレイアウトを整理。「状態」と「設定」を分け、長い説明はツールチップにまとめ、見やすくしました。

### Fixed

- 回答キャッシュの整合性。ページが除外・リンク切れ等になった際、そのページを含む古い回答キャッシュを自動で削除し、削除済みページが再び案内される問題を防ぎます。
- チャットで返信を待たずに連続送信すると、吹き出しの表示順が崩れることがある問題を修正（送信中は次の送信を一時的に無効化）。

## [1.0.0] - 2026-06-29

### Added

- 初回リリース。
- サイト内 AI コンシェルジュ（フローティングチャット）。訪問者の質問に対し、サイト内の最適なページを意味的に検索して案内します。
- sitemap.xml / llms.txt の解析によるページ索引化（カスタムテーブル + WP-Cron 差分更新、手動再構築、除外ルール）。
- 検索 AI（埋め込み）と回答 AI（チャット）の役割分担。プロバイダ／モデルはそれぞれ差し替え可能。
- コスト上限（1 日・1 か月）、リクエスト回数上限、レート制限、トークン上限などの安全装置。
- API キーの暗号化保存（フロント非出力）、個人情報の best-effort マスク、ログ保存日数、同意取得などのプライバシー機能。
- 表示カスタマイズ（ボタン文言・チャットタイトル・回答アイコン画像・テーマカラー・あいさつ・質問例・表示ページ）。スマートフォンではアイコンボタンに自動切替。
- 分析機能（訪問者／管理者テストの分離集計、人気の質問、コンテンツギャップ、案内ページ付き履歴、CSV 出力）。
- 任意の GA4 イベント連携。
- 利用者向け HTML マニュアル（`docs/`）。

[1.0.2]: https://github.com/TsuyoshiKashiwazaki/wp-plugin-kashiwazaki-seo-concierge/releases/tag/v1.0.2
[1.0.1]: https://github.com/TsuyoshiKashiwazaki/wp-plugin-kashiwazaki-seo-concierge/releases/tag/v1.0.1
[1.0.0]: https://github.com/TsuyoshiKashiwazaki/wp-plugin-kashiwazaki-seo-concierge/releases/tag/v1.0.0
